apiVersion: v1
kind: Service
metadata:
  name: { { include "agedigitaltwins.fullname" . } }
  labels: { { - include "agedigitaltwins.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "agedigitaltwins.selectorLabels" . | nindent 4 } }
