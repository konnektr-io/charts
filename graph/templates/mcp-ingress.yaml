{{- if and .Values.mcp.enabled .Values.mcp.ingress.enabled -}}
mcpVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "graph.fullname" . }}-mcp
  labels:
    {{- include "graph.labels" . | nindent 4 }}
  {{- with .Values.mcp.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.mcp.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.mcp.ingress.tls }}
  tls:
    {{- range .Values.mcp.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.mcp.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "graph.fullname" $ }}-mcp
                port:
                  number: {{ $.Values.mcp.service.port }}
          {{- end }}
    {{- end }}
{{- end }}