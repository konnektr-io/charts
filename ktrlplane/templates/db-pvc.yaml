{{- if and .Values.database.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ktrlplane-db-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.database.storage }}
{{- end }}