# This CRD is required for the operator to function. You can install it with:
# kubectl apply -f https://github.com/konnektr-io/db-query-operator/releases/latest/download/crds.yaml
# Or include it in your Helm install by uncommenting below (not recommended for production upgrades).
#
# apiVersion: apiextensions.k8s.io/v1
# kind: CustomResourceDefinition
# ...
